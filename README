#/*
# *  README
# *  Thu Nov 04 10:36:23 2023
# *  Copyright (c)  2001-2024 Gabor E. Tusnady
# *  <tusi@enzim.hu>
# *  All rights reserved.
# */

Quick install of Tmdet package:

> tar -xzvf Tmdet.tgz
> cd Tmdet
> cmake -B build
> make -C build
> sudo make -C build install


CONFIGURATION AND REQUIREMENTS

Environment variables are deined in .env file, but can be set via shell environment variable:

Chemical component directory is important during parsing CIF files. If it is missing, tmdet
downloaded it from WWPDB and install it automatically.
